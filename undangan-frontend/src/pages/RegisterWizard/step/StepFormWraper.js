import { styled } from '@mui/material';

const StepFormWraper = styled('div')(({ theme }) => ({
  height: 630,
  display: 'flex',
  alignItems: 'center',
  overflow: 'auto',
}));

export default StepFormWraper;
